import 'package:flutter/material.dart';

class Message {
  final String msg;
  final String time;
  final bool seen;
  final bool received;
  final bool sent;
  Message(this.msg, this.time, this.seen, this.received, this.sent);
}

class MessageWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Wrap(
      
    );
  }
}
